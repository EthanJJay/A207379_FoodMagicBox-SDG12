org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official

# ? ????????? AndroidX ?? (Core Fix: Explicitly enable AndroidX support)
android.useAndroidX=true
# ? ??????????????????? (Recommended Companion Setting: Automatically migrate old libraries to be compatible with the new architecture)
android.enableJetifier=true